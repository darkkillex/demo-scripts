## If an application outputs logs to the file /log/app.log. View the logs and try to identify the issues so run:

kubectl exec -it app cat /log/app.log  --namespace=NAMESPACE_NAME

You can exec in to the container and open the file:
kubectl exec webapp -- cat /log/app.log